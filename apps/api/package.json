{
  "name": "@repo/api",
  "scripts": {
    "build:types": "tsc --build --watch",
    "build": "vite build"
  },
  "type": "module",
  "dependencies": {
    "@repo/core": "workspace:*"
  },
  "devDependencies": {
    "@repo/vite-config": "workspace:*",
    "typescript": "^5.5.3",
    "vite": "^5.3.3"
  }
}
