{
  "name": "@repo/api",
  "scripts": {
    "build": "rsbuild build",
    "dev": "nodemon --watch dist --exec 'node dist/js/server/index.js'"
  },
  "dependencies": {
    "@repo/core": "workspace:*"
  },
  "devDependencies": {
    "@repo/rsbuild-config": "workspace:*",
    "@rsbuild/core": "^0.7.10",
    "@rsdoctor/rspack-plugin": "^0.3.6",
    "@types/node": "^20.14.9",
    "nodemon": "^3.1.4",
    "typescript": "^5.5.3"
  }
}
